# Skilora Database Configuration
# These are local defaults. For production, use environment variables:
# SKILORA_DB_URL, SKILORA_DB_USER, SKILORA_DB_PASSWORD

db.url=jdbc:mysql://127.0.0.1:3306/skilora?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&connectTimeout=3000&socketTimeout=3000
db.user=root
db.password=

# App Configuration
app.url.base=https://skilora.tn
