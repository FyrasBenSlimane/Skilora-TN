Place the Skilora logo here for PDF reports:
  skilora-logo.png

The image will appear at the top of generated PDF reports.
Recommended size: about 400x120 px (or similar ratio). Format: PNG with transparent or dark background.

If the file is missing, the generator will show the text "SKILORA" with the tagline instead.
