<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.Insets?>
<?import com.skilora.framework.components.*?>

<VBox xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.skilora.recruitment.controller.PostJobController"
      spacing="24"
      style="-fx-background-color: transparent;">
    <padding><Insets top="32" right="32" bottom="32" left="32"/></padding>
    
    <!-- Header -->
    <HBox spacing="24" alignment="CENTER_LEFT">
        <VBox spacing="4">
            <Label text="Publier une Offre d'Emploi" styleClass="h2"/>
            <Label fx:id="stepLabel" text="Étape 1 sur 3 - Informations de base" styleClass="text-muted"/>
        </VBox>
        
        <Region HBox.hgrow="ALWAYS"/>
        
        <TLButton fx:id="saveBtn" text="💾 Enregistrer brouillon" variant="OUTLINE" onAction="#handleSaveDraft"/>
    </HBox>
    
    <!-- Progress Bar -->
    <HBox spacing="8" alignment="CENTER">
        <VBox fx:id="step1Indicator" spacing="4" alignment="CENTER" HBox.hgrow="ALWAYS">
            <Label text="1. Informations" styleClass="text-muted"/>
            <TLProgress fx:id="progress1" progress="1.0" maxWidth="Infinity"/>
        </VBox>
        <VBox fx:id="step2Indicator" spacing="4" alignment="CENTER" HBox.hgrow="ALWAYS">
            <Label text="2. Compétences" styleClass="text-muted"/>
            <TLProgress fx:id="progress2" progress="0.0" maxWidth="Infinity"/>
        </VBox>
        <VBox fx:id="step3Indicator" spacing="4" alignment="CENTER" HBox.hgrow="ALWAYS">
            <Label text="3. Aperçu" styleClass="text-muted"/>
            <TLProgress fx:id="progress3" progress="0.0" maxWidth="Infinity"/>
        </VBox>
    </HBox>
    
    <!-- Content Container -->
    <ScrollPane fitToWidth="true" style="-fx-background-color: transparent;" VBox.vgrow="ALWAYS">
        <StackPane fx:id="contentStack"/>
    </ScrollPane>
    
    <!-- Navigation Buttons -->
    <HBox spacing="12" alignment="CENTER_RIGHT">
        <TLButton fx:id="cancelBtn" text="Annuler" variant="GHOST" onAction="#handleCancel"/>
        <Region HBox.hgrow="ALWAYS"/>
        <TLButton fx:id="prevBtn" text="← Précédent" variant="OUTLINE" onAction="#handlePrevious" visible="false"/>
        <TLButton fx:id="nextBtn" text="Suivant →" variant="PRIMARY" onAction="#handleNext"/>
        <TLButton fx:id="publishBtn" text="🚀 Publier" variant="PRIMARY" onAction="#handlePublish" visible="false"/>
    </HBox>
</VBox>
