<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.Insets?>
<?import com.skilora.framework.components.*?>

<VBox xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.skilora.controller.ForgotPasswordController"
      spacing="24"
      alignment="CENTER"
      style="-fx-background-color: transparent;">
    
    <padding><Insets top="80" right="32" bottom="80" left="32"/></padding>
    
    <!-- Card Container -->
    <TLCard maxWidth="480" alignment="CENTER">
        <VBox spacing="24" alignment="CENTER">
            <padding><Insets top="32" right="32" bottom="32" left="32"/></padding>
            
            <!-- Header -->
            <VBox spacing="8" alignment="CENTER">
                <Label text="🔐" style="-fx-font-size: 48px;"/>
                <Label text="Mot de passe oublié?" styleClass="h2"/>
                <Label text="Entrez votre email et nous vous enverrons un lien de réinitialisation." 
                       styleClass="text-muted" wrapText="true" alignment="CENTER"/>
            </VBox>
            
            <!-- Email Input -->
            <VBox spacing="8">
                <Label text="Email" styleClass="label"/>
                <TLTextField fx:id="emailField" promptText="votre@email.com" prefWidth="400"/>
                <Label fx:id="errorLabel" text="" styleClass="text-destructive" visible="false" managed="false"/>
                <Label fx:id="successLabel" text="" style="-fx-text-fill: #22c55e;" visible="false" managed="false"/>
            </VBox>
            
            <!-- Actions -->
            <VBox spacing="12">
                <TLButton fx:id="sendBtn" text="Envoyer le lien" variant="PRIMARY" 
                         onAction="#handleSendReset" maxWidth="Infinity"/>
                <TLButton fx:id="backBtn" text="← Retour à la connexion" variant="GHOST" 
                         onAction="#handleBack" maxWidth="Infinity"/>
            </VBox>
            
            <!-- Additional Info -->
            <Separator/>
            
            <VBox spacing="8" alignment="CENTER">
                <Label text="Vous n'avez pas reçu l'email?" styleClass="text-muted"/>
                <TLButton fx:id="resendBtn" text="Renvoyer" variant="OUTLINE" 
                         onAction="#handleResend" disable="true"/>
            </VBox>
        </VBox>
    </TLCard>
</VBox>
